# AWS credentials and keys — never commit these
*.pem
*.ppk
.aws/
credentials
aws-credentials*

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform.lock.hcl
crash.log

# Environment
.env
.env.*
*.env

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db

# Large image files — compress before committing
# (keeps git clone fast for reviewers)
*.psd
*.ai
